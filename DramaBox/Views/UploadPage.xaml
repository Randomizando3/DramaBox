<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="DramaBox.Views.UploadPage"
    Title="Criar"
    BackgroundColor="{StaticResource Bg}">

    <Grid Padding="16">
        <Frame BackgroundColor="{StaticResource Card}"
               CornerRadius="18"
               Padding="16"
               HasShadow="False">

            <VerticalStackLayout Spacing="10">
                <Label Text="Criar / Enviar"
                       FontAttributes="Bold"
                       FontSize="20" />

                <Label Text="Base pronta. Aqui você integra upload (Storage) e cadastro (RTDB)."
                       TextColor="{StaticResource Muted}" />

                <Button Text="Abrir plano/assinatura"
                        Clicked="OnGoSub" />
            </VerticalStackLayout>

        </Frame>
    </Grid>
</ContentPage>
